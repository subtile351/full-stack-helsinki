{
  "name": "backend",
  "version": "1.0.0",
  "description": "Backend of the phonebook",
  "main": "index.js",
  "scripts": {
    "start": "node index.js",
    "dev": "node --watch index.js",
    "test": "echo \"Error: no test specified\" && exit 1",
    "build:ui": "if exist ..\\backend\\dist rd /s /q ..\\backend\\dist & cd ../frontend & npm run build & xcopy dist ..\\backend\\dist /E /I /Y",
    "deploy:full": "npm run build:ui && git add . && git commit -m uibuild && git push"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "dotenv": "^16.5.0",
    "express": "^5.1.0",
    "mongoose": "^8.13.2",
    "morgan": "^1.10.0"
  }
}
